EXPO_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=
# Liens de confirmation e-mail (Apple 2.1) : dans Supabase Dashboard → Authentication → URL Configuration,
# autoriser Redirect URLs pour votre schéma (app.json expo.scheme), ex. dabars://** et l’URL exacte
# renvoyée par expo-linking createURL('login') sur une build iOS release (souvent dabars:///login).
#
# Extraction IA (Gemini 2.5 Flash) — clé UNIQUEMENT côté Supabase (pas dans l’app) :
#   1. Créer une clé sur https://aistudio.google.com/apikey
#   2. npx supabase secrets set GEMINI_API_KEY=AIza... --project-ref tqvxwthzpahwcscpwyrr
#   3. npx supabase functions deploy extract-receipt --project-ref tqvxwthzpahwcscpwyrr
